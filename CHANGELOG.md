# 更新日志

本文件记录 HugoAura-Install 的版本更新。

## [Rel] v0.0.4-beta-I - 2026-08-15

### 新增

- 一键更新功能：检测到 HugoAura 已安装且存在新版本时，显示“一键更新”按钮；已是最新版本时隐藏该按钮并提示“已是最新版本”。
- 自动检测更新：启动时异步对比注册表中的已安装版本与最新稳定版，有更新时显示版本差异。
- 资源文件 SHA256 校验：下载 `aura.zip` / `core.zip` 后，与 GitHub API 提供的 release 摘要比对，防止代理污染或文件损坏。
- 新增 `CHANGELOG.md`，开始维护版本更新记录。

### 改进

- 下载源列表扩充并重新排序：加入多个实测可用且稳定的 GitHub 镜像源，按速度排序，保留官方直连兜底。
- 下载器增加 URL 安全校验：仅允许 `http/https`，并在请求前拒绝环回、私有、保留等非公网地址。
- 状态机重构：使用统一的 `current_operation` 区分安装、更新、卸载，避免多个布尔标志同时为真的歧义；取消时由 worker 统一清理状态，修复旧线程覆盖新操作状态的竞态。
- `--ci` 参数修复：CLI 指定 CI 构建时不再被强制改成最新稳定版。
- CI 版本信息动态识别：`versions.json` 生成脚本不再硬编码 `vAutoBuild`，改为从 GitHub releases 中选取最新的 `vAutoBuild-*` 构建。
- ASAR 解包错误处理：解包失败会整体报错并记录失败文件，不再在残缺目录上继续打包。
- UAC 提权参数修正：不再把 exe 自身路径重复传给新进程，避免 argparse 参数解析失败。
- 本地安装注册表版本记录修正：本地路径安装时写入 `local`，避免被误判为存在新版本。
- 下载进度回调在安装流程结束统一清理，避免失败时残留。
- 版本号对齐：`pyproject.toml` 与 `version_info.txt` 统一到 `0.0.4-beta-I` 系列。

### 修复

- 修复 `--ci` 命令行参数被忽略的问题。
- 修复 CI 版本信息写入不存在的固定 tag 的问题。
- 修复 ASAR 解包单文件失败被静默忽略的问题。
- 修复取消操作后旧线程可能覆盖新操作状态的问题。
- 修复本地安装被误判为“有可用更新”的问题。
- 修复提权重启时重复传递 exe 路径导致参数解析失败的问题。
- 修复下载失败后全局进度回调残留的问题。
- 修复日志中打印整个进程名列表而非当前进程名的问题。

### 已知说明

- 安装器版本显示为 `[Rel] v0.0.4-beta-I`；PEP 440 项目版本保持 `0.0.4`。
- 下载源可达性依赖外部镜像站，安装器会自动按顺序尝试并回退到官方 GitHub 直连。
